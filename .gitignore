# Rust
/target
**/target
Cargo.lock.bak

# Tauri generated
/src-tauri/gen
/src-tauri/target

# Node (frontend tooling, if ever added)
node_modules
dist

# OS / editor
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# envyou local state (never commit secrets)
enc_state.json
*.tmp

# License signing keys — the private key must NEVER be committed
*-signing.key
*.signing.key
envyou-signing.key
